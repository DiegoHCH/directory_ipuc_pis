name = "ipuc-notifications"
main = "worker.js"
compatibility_date = "2024-09-23"
workers_dev = true

# Secrets — configurar con wrangler secret put:
#   wrangler secret put FIREBASE_SERVICE_ACCOUNT
#   wrangler secret put FIREBASE_PROJECT_ID
#
# WORKER_SECRET quedó obsoleto: la autenticación es por ID token de Firebase.
# Eliminar el secreto viejo con: wrangler secret delete WORKER_SECRET
