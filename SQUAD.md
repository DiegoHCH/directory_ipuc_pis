# SQUAD.md — pizarra del squad

- 2026-07-25 — Santiago: arreglado el botón de compartir en el perfil de otros miembros (`member_profile_screen.dart` tenía `onShare: () {}` vacío). Ahora comparte el mismo deep link `…/member?id=<id>` que ya usaba mi perfil, con fallback a portapapeles. Además moví el texto del share a l10n (`shareMemberText` con placeholders `{name}` y `{church}`, más `shareLinkCopied`) en en/es. El nombre "IPUC Pisarreal" NO se traduce: sale de `dirChurchShort`, que es la fuente única que ya usan directory_screen y register_screen. También pasé `settingsHelpMessage` (mensaje de soporte por WhatsApp) al mismo patrón. Convención para el squad: el nombre de la iglesia NUNCA se escribe literal en un `.arb`, se inyecta como placeholder `{church}` desde `dirChurchShort`. `flutter analyze` limpio.
- 2026-07-25 — Santiago: SEGURIDAD. El `WORKER_SECRET` estaba hardcodeado en `notification_service.dart` y viajaba dentro del APK, así que cualquiera podía extraerlo y mandar push a todos los usuarios. Se reemplazó por autenticación con ID token de Firebase verificado en el Worker contra el JWKS de Google (gratis, sin Cloud Functions / plan Blaze). Añadido CORS al Worker: la app web nunca pudo llamarlo por falta de preflight, fallaba en silencio. `.wrangler/` y `.dev.vars` salieron del índice y entraron al `.gitignore`. Pruebas en `cloudflare/verify_id_token.test.mjs` (`node verify_id_token.test.mjs`).
  PENDIENTE DE DIEGO (no lo hice yo): desplegar el worker, `wrangler secret delete WORKER_SECRET`, y publicar build de app. El secreto viejo sigue en el historial git y en los builds ya distribuidos: darlo por quemado.
